export enum StrategyName {
  JwtUserStrategy = 'JWT_USER_STRATEGY',
  JwtStaffStrategy = 'JWT_STAFF_STRATEGY',
}

export enum TransactionType {
  Deposit = 'DEPOSIT',
  Withdraw = 'WITHDRAW',
  Transfer = 'TRANSFER',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}
